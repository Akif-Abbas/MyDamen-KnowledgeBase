import documentIcon from "../../assets/document.svg";
import educationIcon from "../../assets/education.svg";
import forumIcon from "../../assets/forum.svg";
import shipIcon from "../../assets/ship.svg";

/**
 * FAQ card icons. Figma MCP asset URLs are not reliable in the browser (short-lived);
 * these are bundled with the app so images always load in dev and on GitHub Pages.
 */
export const FAQ_ICON_SHIP = shipIcon;
export const FAQ_ICON_DOCUMENT = documentIcon;
export const FAQ_ICON_EDUCATION = educationIcon;
export const FAQ_ICON_FORUM = forumIcon;
